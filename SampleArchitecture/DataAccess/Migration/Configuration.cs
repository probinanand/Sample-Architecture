namespace TestArchitecture.DataAccess.Migration
{
    using System.Data;
    internal sealed class Configuration //:DbMigrationConfiguration<TestArchitectureContext>
    {
        //DbContextOptionsBuilder<ApplicationDbContext>(); 
        //options.UseSqlServer(Configuration.GetConnectionString("DefaultConnection"));
        //using (var context = new ApplicationDbContext(options.Options)) { context.Database.Migrate(); }
}
}